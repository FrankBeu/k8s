#!/usr/bin/env zsh
###
##
# * REPOSITORY
#
##
###
echo -ne 'Enter your harbor.thesym.site credentials \nUsername: ' && read HARBOR_USERNAME
echo -ne 'Password: ' && read HARBOR_PASSWORD
helm repo add --username=${HARBOR_USERNAME} --password=${HARBOR_PASSWORD} thesym.site https://harbor.thesym.site/chartrepo
helm repo update
