apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "powerDns.fullname" . }}-test-connection"
  labels:
    {{- include "powerDns.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "powerDns.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
